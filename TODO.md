# TODO — My Little Plant

Открытые задачи и идеи. Отмечай: `[x]` — одобрено в работу, `~~зачёркнуто~~` —
отклонено. Сгруппировано по приоритету. **v1.0.0 live** в Chrome Web Store
(`nmfppnlojcllbcokdpbcfaofhdbckoaa`); **v1.1.0 отправлена на ревью** (перелив,
статистика, кнопки настроек, баланс воды, глифы). Следующий патч — 1.1.1.

> Выполненные пункты не копятся здесь — история в git и `DECISIONS.md`.

---

## A. Полировка к патчу 1.1.1 (мелкое)

- [ ] **Иконка расширения на 16px.** Маскот хорош на 128, но на 16px в тулбаре
      мелковат — проверить читаемость силуэта, при нужде упростить отдельный 16px.
- 🔄 **Иконка статуса `unwell` (болезнь/сухие листья).** Сейчас = дубль мокрого
      `overwater`. Промпт готов (`DESIGN-PROMPT.md`, доп. к ПАЧКЕ 6) — поникший
      бурый росток. Ждём генерацию → перезаписать `icons/status/unwell.png`.
- [ ] **Оптимизация размера билда (~1 МБ → цель ~350-450 КБ).** Размер
      некритичен для CWS (лимит 2 ГБ, на показы не влияет) — это гигиена.
      - ✅ `water-marker.png` уже ужат 108→8 КБ (был overscale 214×290 ради 14px).
      - Иконки = тысячи цветов (сглаживание от нейросети), lossless indexed
        невозможен → нужен **pngquant** (near-lossless, `brew install pngquant`),
        `magick` тут не помощник.
      - Шрифт `Jersey25.ttf` 140 КБ → subset (ASCII+`°`) + woff2 ≈ 15 КБ
        (нужен `fonttools`/`woff2`), правка `@font-face` в `styles.css`.
      - `tools/pack.sh` теперь печатает размер билда и WARN по файлам >50 КБ —
        следить, чтобы супержирное не проскакивало.

## B. Геймплей / фичи (бэклог, обсудить объём)

- [ ] **Погода / здоровье — баланс для теплолюбивых.** При decay=1.0 вода раз в
      сутки комфортна, но теплолюбивые (basil/tomato/sunflower) в холодные блоки
      проседают по health до ~0 даже при идеальном поливе (temp-штраф -0.4/ч).
      Radish (любит холод) не страдает. Решить: смягчить `tempHealth`, или
      сделать холод реже/короче, или оставить как «погодный вызов». Перепроверить
      симуляцией.
- [ ] **Больше растений** — новые семечки (форма, характер, факты, баланс).
- [ ] **Звуки** — спорно: игра намеренно «тихая, без уведомлений». Возможно, не нужно.
- [ ] **Локализация (RU и др.)** — additive, строки сейчас инлайн в `render.js`/
      `settings.js`; вынести в i18n-слой по locale.

## C. Визуал растения — следующее крупное направление

- [ ] **Пиксельный рендеринг растения вместо процедурного SVG + лёгкое
      покачивание.** Сейчас `plantSVG` рисует стебель/листья/цветок кодом. Цель:
      настоящий пиксель-арт — спрайт-лист стадий роста (промпт в
      `DESIGN-PROMPT.md`), накладывать на PNG-горшок. Плюс **idle-анимация**:
      растение слегка покачивается изредка, когда на него смотришь (CSS/transform,
      ненавязчиво, не каждую секунду). Крупная задача — делать по одному растению.
- [ ] **Визуал гнили в `plant-svg.js`** — потемневший стебель/поникшие листья при
      высоком `rot` (заодно с пиксель-рендерингом).

---

> Перед любым изменением движка/погоды — перепроверять баланс временным
> `sim-balance.mjs` (см. `DECISIONS.md`). Сохранёнки не ломать: поля — в
> `defaultState()`, несовместимость — через `migrate()`.
> Упаковка — только `bash tools/pack.sh` (whitelist + гейт DEV + проверка веса).
