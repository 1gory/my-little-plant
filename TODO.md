# TODO — My Little Plant

Открытые задачи и идеи. Отмечай: `[x]` — одобрено в работу, `~~зачёркнуто~~` —
отклонено. Сгруппировано по приоритету. v1.0.0 опубликована (на ревью);
ближайшая цель — патч 1.0.1 во время ревью.

---

## A. Полировка к патчу 1.0.1 (мелкое, до первых юзеров)

- [ ] **Эмодзи в текстовых экранах.** Остались `🎉 / 🥀` (финал), `🌼` («Did you
      know?»), `🌱` (подзаголовок «Let's plant the seed», кнопка «Plant again»).
      На фоне пиксель-арта смотрятся инородно. Решить: убрать / заменить на
      пиксель-иконки / оставить. *(косметика, быстро)*
- [ ] **Иконка расширения на 16px.** Маскот хорош на 128, но на 16px в тулбаре
      мелковат — проверить читаемость силуэта, при нужде упростить отдельный 16px.
- [ ] **Промо-тайл 440×280** для листинга (опционально, помогает в выдаче).
- [ ] **Финальная проверка старого сейва** перед заливкой патча (hydrate/migrate
      уже стоит — просто прогнать: открыть со старым состоянием, не сбросилось ли).

## B. Геймплей / фичи (бэклог, обсудить объём)

- [ ] **Экран/блок статистики после финала** — сколько раз поливал, обрезал,
      пересаживал. Требует счётчиков в `state` (+ миграция, безопасно).
- [ ] **Больше растений** — новые семечки (форма, характер, факты, баланс).
- [ ] **Анимации** — idle-покачивание растения, капля при поливе, эффект
      обрезки/пересадки. *(оживляет, но объёмно)*
- [ ] **Звуки** — спорно: игра намеренно «тихая, без уведомлений». Возможно, не нужно.
- [ ] **Локализация (RU и др.)** — additive, строки сейчас инлайн в `render.js`/
      `settings.js`; вынести в i18n-слой по locale.

## C. Дизайн-сессия (отдельно, не код-логика)

- [x] **«Стол» на экране роста** — ✅ сделано (дизайн-сессия 2026-05-29): стол =
      нижний бордюр рамки сцены (`frame-scene`), горшок стоит на нём через
      `translateY`. Заодно весь экран роста приведён к концепту `art.png`:
      пиксельные рамки, плашка погоды, статы в рамках, статус с настроением,
      выровненные кнопки, факты на финале. Детали — в `DECISIONS.md` («Визуал»).
- [ ] **Растение — пиксель-арт вместо процедурного SVG.** Сейчас `plantSVG`
      рисует стебель/листья/цветок кодом. Идея: спрайт-лист стадий роста (промпт
      уже есть в `DESIGN-PROMPT.md`), накладывать на PNG-горшок. Крупная задача.
- [x] **Чистка мёртвого CSS** — ✅ сделано: удалены `.card-emoji`, `.meta-row`,
      `--pix` polygon (от старого clip-path-подхода), `@keyframes pulse` и
      устаревшие комментарии. Скобки сбалансированы, рендер не сломан.

## D. Инфраструктура / публикация

- [ ] **GitHub repo + Pages** (если ещё не залито) — privacy-policy URL нужен для
      листинга; `docs/` готов (`index.html`, `privacy-policy.html`).
- [x] **Удалить `my-little-plant-v1.0.0.zip` из рабочей папки** — ✅ удалён
      (пересобирается по `RELEASE.md` перед патчем).

---

> Перед любым изменением движка/погоды — перепроверять баланс временным
> `sim-balance.mjs` (см. `DECISIONS.md`). Сохранёнки не ломать: поля — в
> `defaultState()`, несовместимость — через `migrate()`.
