// Глобальные настройки и баланс игры.
// Меняй цифры здесь, чтобы подкрутить темп игры.

export const HOUR = 60 * 60 * 1000;
export const DAY = 24 * HOUR;

// Сколько реальных дней до полного вырастания при хорошем уходе.
export const GAME_DAYS = 30;

// Базовый прирост роста (%) в час при идеальных условиях.
export const BASE_GROWTH_PER_HOUR = 0.18;

// Базовый расход воды (%) в час. Модифицируется погодой, горшком и семечкой.
export const WATER_DECAY_PER_HOUR = 2.0;

// Сколько влаги добавляет одно нажатие «Полить». Небольшая порция — чтобы
// дозировать влажность точнее (полный горшок со старта — примерно 3–4 полива).
export const WATER_PER_POUR = 15;

// Глубина горшка по уровням пересадки: до какого % роста можно дорасти.
export const POT_TIERS = [30, 55, 80, 100];

// Погода держится блоками по столько часов.
export const WEATHER_BLOCK_HOURS = 14;

// Сколько часов с нулевым здоровьем подряд = растение засохло.
export const WITHER_HOURS = 72;

// Порог накопленных сухих листьев, после которого начинаются штрафы.
export const DRIED_LEAVES_THRESHOLD = 3;

// Если вода ниже порога семечки — рост останавливается, здоровье падает.
// Множители усталости/восстановления здоровья.
export const HEALTH_RECOVER_PER_HOUR = 0.5;

// Максимум часов, которые симулируем за один проход (защита от зависания
// при очень долгом отсутствии).
export const MAX_CATCHUP_HOURS = 60 * 24 + 240; // запас для самой долгой семечки (60 дней)

// Режим разработчика: показывает кнопки перемотки времени.
// Поставь false для «честной» игры на месяц.
export const DEV = false;

export const STORAGE_KEY = 'mlp_state_v1';
